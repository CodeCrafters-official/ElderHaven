import 'package:flutter/material.dart';

class PatientProfilesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Patient Profiles'),
      ),
      body: Center(
        child: Text('Patient Profiles Screen'),
      ),
    );
  }
}
